# Configuraciones para todo el proyecto.

ingest:
  include_dirs: []
  exclude_dirs: []
  max_chars: 1000
  overlap_chars: 200

embed:
  model_name: "BAAI/bge-m3"
  max_length: 1024
  use_fp16: true
  batch_size: 8
qdrant:
  collection_name: "rag_obsidian"
  host: "localhost"
  port: 6333
